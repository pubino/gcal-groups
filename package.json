{
  "name": "gcal-groups",
  "version": "1.0.0",
  "description": "Chrome extension for managing Google Calendar groups",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:security": "node tests/security-audit.test.js",
    "package": "node scripts/package.js"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
